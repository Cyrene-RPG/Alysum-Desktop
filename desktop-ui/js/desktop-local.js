/** Re-export from site module (single source of truth). */
export {
  DESKTOP_LOCAL_HOST_KEY,
  isDesktopLocalHost,
  enableDesktopLocalHost,
  clearDesktopLocalHost,
} from "/js/desktop-auth.js?v=1";
